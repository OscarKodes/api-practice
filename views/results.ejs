<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><%= name[0].toUpperCase() + name.slice(1) %></title>
  </head>
  <body>

    <h1>Pokemon No. <%= id %></h1>

    <h3>Name: <%= name[0].toUpperCase() + name.slice(1) %></h3>

    <p>Sprites</p>
    <% for (let key in sprites) { %>
      <% let imageURL = sprites[key]; %>
      <% if (imageURL !== null) { %>
        <img src="<%= imageURL %>" alt="">
      <% } %>
    <% } %>

</html>
